<UserControl x:Class="Aurora.EndPoints.Procyon.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Aurora.EndPoints.Procyon.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="200">
    <StackPanel HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Margin="10">
        <TextBlock Text="Главное меню"
                   FontSize="24"
                   HorizontalAlignment="Center"
                   Margin="0 0 0 20" />
        <Separator Background="Black"
                   Margin="0,0,0,10" />
        <Button Content="Открыть запрет"
                Command="{Binding StartZapret}"
                Width="150" Height="40" />
        <Button Content="Закрыть запрет"
                Command="{Binding CloseZapret}"
                Width="150" Height="40"
                Margin="0,5,0,0" />
    </StackPanel>
</UserControl>